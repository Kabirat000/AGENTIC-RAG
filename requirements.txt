# Web APIs / servers
fastapi
uvicorn[standard]
flask
python-multipart   # file uploads (forms)

# HTTP client (frontend → backend calls)
requests

# Env/config management
python-decouple

# Vector database client
qdrant-client

# LLM providers / reasoning / web search
google-generativeai   # Gemini
groq                  # Llama via Groq
tavily-python         # web snippets

# Document parsers
pypdf
python-docx
beautifulsoup4

# Retrieval / reranking stack
sentence-transformers
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Optional (validation helpers; FastAPI uses it)
pydantic
